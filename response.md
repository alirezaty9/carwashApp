<div dir="rtl" align="right">

# ✅ تأییدِ حداقلِ طولِ رمز — ۴ کاراکتر

<div style="background:#ebfbee;border-right:4px solid #2f9e44;color:#14532d;padding:8px 12px;border-radius:6px">✅ جوابت ثبت شد. <b>هیچ تغییری در کد لازم نبود</b> — عدد از قبل روی ۴ بود، پس همان‌طور می‌ماند.</div>

<br>

---

<br>

## آن عدد کجای اپ اثر می‌گذارد

**📍 کدام قسمتِ اپ:** صفحه‌ی **«یک رمزِ تازه بگذارید»** — همانی که وقتی کاربری با رمزِ پیش‌فرض وارد می‌شود قبل از صندوق می‌آید.

**🎯 کارش:** اگر رمزِ واردشده کمتر از ۴ کاراکتر باشد، زیرِ کادرِ تکرارِ رمز پیامِ قرمزِ **«رمز باید حداقل ۴ کاراکتر باشد.»** نشان داده می‌شود و فرم ثبت نمی‌شود.

**سه قاعده‌ی دیگر هم کنارِ آن سرِ جایشان هستند:** رمزِ جدید نمی‌تواند همان رمزِ پیش‌فرض باشد · نمی‌تواند رمزِ پشتیبانیِ تو باشد · و تکرارِ رمز باید با خودش بخواند.

<br>

---

<br>

## ❓ تصمیم‌های با تو

**۱. ابزارِ تست را به نسخه‌ی ۳ ارتقا بدهم؟** *(سه نوبت است بی‌جواب مانده — ولی فوری نیست)*
   - 🟢 **پیشنهادِ من:** فعلاً نه. ابزارِ تستِ فعلی به نسخه‌ی قدیمی‌ترِ موتورِ بیلد وابسته است، پس دو نسخه‌ی موازی نصب می‌شوند. الان با محدودکردنِ دامنه‌ی بررسیِ خطا به کدِ خودِ برنامه دورش زده‌ام که بی‌ریسک است. ارتقای واقعی فقط با اجرای کلِ تست‌ها قابلِ تأیید است و من تست اجرا نمی‌کنم — یعنی چیزی تأییدنشده تحویلت می‌دادم، آن هم درست وقتی داری عملیاتی می‌کنی.
   - ⚪ **گزینه‌ی دیگر:** بگو انجام بدهم؛ بعدش خودت `npm test` را بزن و خروجی را بفرست تا اگر چیزی شکست درستش کنم.
   - 📌 **اگر جواب ندهی:** همین‌طور می‌ماند و هیچ مشکلی برای اپ یا فایلِ نصبی ایجاد نمی‌کند.

<br>

📌 **بقیه‌ی کارهای بازت در `pending.md` است** — مهم‌ترینشان دو مورد است که هنوز انجام نشده‌اند: پشتیبان‌گیری از کلیدِ لایسنس، و اجرای `npm test` بعد از تغییراتِ این چند نوبت.

<br>

---

<br>

## 📋 خلاصه — فقط همین‌ها را انجام بده

**۱. من چه کردم** (تو کاری نداری):
- جوابت را ثبت کردم؛ حداقلِ طولِ رمز روی **۴ کاراکتر** ماند و کد دست نخورد.
- آن سؤال را از `pending.md` برداشتم.

**۲. تو باید این‌ها را اجرا کنی:**

```bash
# ۱) تأیید اینکه تغییراتِ چند نوبتِ اخیر چیزی را نشکسته — باید همان ۷۰ تست سبز بماند
npm test

# ۲) ساختِ نصبیِ نسخه‌ی ۱.۰.۰
npm run electron:build:win
```
⬅️ در **ترمینالِ خودت** (پنلِ پایینِ VS Code، تبِ `TERMINAL`) بزن. اگر دستورِ اول قرمز شد، جلوتر نرو و خروجی را برایم بفرست.

**۳. کارهای بیرونِ ترمینال:**
- [ ] 🔴 از `yatash-private.pem` در دو جای مستقل پشتیبان بگیر — هنوز انجام نشده و مهم‌ترین کارِ بازت است.
- [ ] امتحان‌های بخشِ چاپ و لایسنس که در `pending.md` فهرست شده‌اند.

<br>

---

<br>

```bash
git add -A && git commit -m "docs(auth): confirm 4-character minimum password length"
```

**چرا این واژه‌ها؟**
- **`docs`** نه `feat` یا `fix`: چون در این نوبت **هیچ خطی از کدِ برنامه عوض نشد** — فقط یک تصمیم تأیید و در فایل‌های یادداشت ثبت شد. اگر `fix` می‌گذاشتم، بعداً که تاریخچه را می‌خوانی فکر می‌کردی اینجا چیزی خراب بوده و درست شده.
- **`(auth)`** داخلِ پرانتز: به این «scope» می‌گویند، یعنی **کدام بخشِ پروژه**. قاعده‌ی طولِ رمز زیرِ چترِ «هویت و دسترسی» است، پس با کامیتِ قبلی هم‌خانواده می‌ماند و بعداً کنارِ هم پیدایشان می‌کنی.

</div>
